import styled from 'styled-components';

export const Button = styled.button`
  border: 1px solid darkgray;
  border-radius: 2px;
  padding: 2px 12px;

  &:hover,
  &:active {
    border-color: gray;
  }
`;
